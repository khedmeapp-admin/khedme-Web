# 🧠 Environment setup for Khedme Web (Next.js frontend)

# Fix for file watching issues on Windows
CHOKIDAR_USEPOLLING=true

# Allow local HTTPS for API calls during development (safe to keep)
NODE_TLS_REJECT_UNAUTHORIZED=0

# 🌐 Backend API base URL
NEXT_PUBLIC_API_BASE_URL=https://khedme-api.onrender.com
